# 1. Kafka
cd kafka_service && docker compose up -d

# 2. ScyllaDB
cd ../data_sink_service && docker compose up -d
docker exec -i scylladb cqlsh < init.cql   # after ~60s when ScyllaDB is healthy

# 3. Flink
cd ../flink_service && docker compose -f docker-compose.flink.yml up -d --build

# 4. Producers
cd ../data_source_servie && docker compose up -d --build
